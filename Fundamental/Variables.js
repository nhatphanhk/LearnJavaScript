/**
 * Rule to get name for variable declarations.
    - Name for variable declarations is case sensitive.
    - Variable names must start with a letter, underscore (_), or dollar sign ($).
    - Variable names can not be a reserved keyword in JavaScript.
 * Var is variable declaration.
 * Let is block-scoped variable declaration.And using to assign value to variable.
 * Const is constant and block-scoped variable declaration.And using to assign constant value to variable.
 * Can declate multiple variables in one statement by using comma(,).
 * distinguish between undefined and undeclarded variables is that 
    - undefined variables are declared but have not been assigned a value, 
    - while undeclared variables have not been declared in the current scope.
 */
